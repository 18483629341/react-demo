const defaultState = {
    count: 0
}

const changeState = (action) => {

}


// renderDom
const render = () => {

}


const dispatch = (action) => {
    changeState(action)
    render()
}